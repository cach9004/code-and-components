doctype html
html
  head
    title cell padding
    style.
      * {box-sizing: border-box;}
      div.title {
      overflow: hidden;
      background-color: #B0E0E6;
      /*position: fixed;*/
      padding: 3px;
      border-style: solid hidden solid hidden;
      border-color: white;
      border-width: 3px;
      }
      .title h1{
      margin-left: 30px;
      float:left;
      color: #565063;
      padding: 0px;
      display: block;
      text-align: center;
      font-size: 30px;
      line-height: 68%;
      text-decoration: underline;
      }
      img {
      float: left;
      display: block;
      max-width: 60px;
      height:auto;
      margin-left: 10px;
      }
      .title a{
      float: right;
      display: block;
      color: #565063;
      text-align: center;
      padding: 20px 20px;
      text-decoration: none;
      font-size: 16px;
      text-decoration: underline;
      }
      div.body{
      background-color:#FFF;
      overflow: hidden;
      border-style: solid hidden solid hidden;
      border-width: 4px;
      border-color: #B0E0E6;
      padding: 10px;
      }
      .body h2{
      color: #333;
      text-align: center;
      font-size: 30px;
      padding: 8px 0px 0px 0px;
      text-decoration: underline;
      }
      .body p{
      text-align: center;
      color: #333;
      font-size: 20px;
      }
      .body a{
      color: #333;
      font-size: 16px;
      padding: auto;
      text-decoration: underline;
      text-align:left;
      margin-right:310px;
      }
      .body a:hover{
      background-color: #1F618D;
      }
      .body .search-container {
      overlfow:hidden;
      display:block;
      margin-left:auto;
      margin-right:auto;
      text-align: center;
      padding: 12px;
      }
      .body input[type=text] {
      padding: 14px 200px 14px 6px;
      margin-top: 8px;
      font-size: 16px;
      border: solid 1px gray;
      text-align: left;
      }
      .body .search-container button {
      float: auto;
      padding: 14px;
      margin-top: 8px;
      margin-right: 20px;
      background: #D1F2EB;
      font-size: 16px;
      border: solid gray 1px;
      cursor: pointer;
      }
      .body .search-container button:hover {
      background: #ccc;
      }
      @media screen and (max-width: 600px) {
      .body .search-container {
      float: none;
      }
      .body input[type=text], .body .search-container button {
      float: none;
      display: block;
      text-align: left;
      width: 100%;
      margin: 0;
      padding: 16px;
      }
      .body input[type=text] {
      border: 1px solid #ccc;
      }
      }
      #map {
      height: 100%;
      }
      /* Optional: Makes the sample page fill the window. */
      html, body {
      height: 100%;
      margin: 0;
      padding: 0;
      }
      #right-panel {
      font-family: 'Roboto','sans-serif';
      line-height: 30px;
      padding-left: 10px;
      }
      #right-panel select, #right-panel input {
      font-size: 15px;
      }
      #right-panel select {
      width: 100%;
      }
      #right-panel i {
      font-size: 12px;
      }
      #right-panel {
      font-family: Arial, Helvetica, sans-serif;
      position: absolute;
      right: 5px;
      top: 95%;
      margin-top: -195px;
      height: 330px;
      width: 200px;
      padding: 5px;
      z-index: 5;
      border: 1px solid #999;
      background: #fff;
      }
      h2 {
      font-size: 22px;
      margin: 0 0 5px 0;
      }
      ul {
      list-style-type: none;
      padding: 0;
      margin: 0;
      height: 271px;
      width: 200px;
      overflow-y: scroll;
      }
      li {
      background-color: #f1f1f1;
      padding: 10px;
      text-overflow: ellipsis;
      white-space: normal;
      overflow: visible;
      }
      li:nth-child(odd) {
      background-color: #fcfcfc;
      }
      #more {
      width: 100%;
      margin: 5px 0 0 0;
      }
  body
    .title
      .gfg
        p#my-image
          img(src='http://lotushospital.co.in/Images/Hospital/Doctor/Small/7_11_group_doctors4.png')
      h1  DoctorDetails.com 
      a(href='/users/login') Log In
      a(href='/registration') Sign Up
    .body
      h2 Looking for a doctor?
      p Search Now
      .search-container
        form(action='/index/search_by_location' method = 'get')
          input(type='text', placeholder='Search by doctor', name='search')
          button(type='submit') Search
        a(href='/index/search_by_location') Or try an advanced search...
      .search-container
        form(action='/index/search_by_location' method = 'get')
          input(type='text', placeholder='Search by hostpital', name='search')
          button(type='submit') Search
        a(href='#') Or try an advanced search...
      .search-container
        form(action='/index/search_by_location' method = 'get')
          input(type='text', placeholder='Search by ailment', name='search')
          button(type='submit') Search
        a(href='#') Or try an advanced search...
    title Geolocation
    meta(name='viewport', content='initial-scale=1.0, user-scalable=no')
    meta(charset='utf-8')
    style.
      /* Always set the map height explicitly to define the size of the div
      * element that contains the map. */
      #map {
      height: 100%;
      }
      /* Optional: Makes the sample page fill the window. */
      html, body {
      height: 100%;
      margin: 0;
      padding: 0;
      }
    #map
    #right-panel
      h2 Results
      ul#places
      button#more More results
    script.
      // Note: This example requires that you consent to location sharing when
      // prompted by your browser. If you see the error "The Geolocation service
      // failed.", it means you probably did not give permission for the browser to
      // locate you.
      var map, infoWindow;
      var pos;
      function initMap() {
      var pyrmont = {lat: 40.0150, lng: -105.2705};
      map = new google.maps.Map(document.getElementById('map'), {
      center: {lat: 40.0150, lng: -105.2705},
      zoom: 10
      });
      // Try HTML5 geolocation.
      if (navigator.geolocation) {
      navigator.geolocation.getCurrentPosition(function(position) {
      pos = {
      lat: position.coords.latitude,
      lng: position.coords.longitude
      };
      console.log("geolcation1");
      pyrmont = pos;
      searchLocations(pos);
      }, function() {
      handleLocationError(true, infoWindow, map.getCenter());
      });
      } else {
      // Browser doesn't support Geolocation
      handleLocationError(false, infoWindow, map.getCenter());
      }
      }
      function searchLocations(pos){
      infoWindow = new google.maps.InfoWindow;
      infoWindow.setPosition(pos);
      infoWindow.setContent('Location found.');
      infoWindow.open(map);
      console.log("above service");
      // Create the places service.
      var service = new google.maps.places.PlacesService(map);
      var getNextPage = null;
      console.log("above service");
      var moreButton = document.getElementById('more');
      moreButton.onclick = function() {
      moreButton.disabled = true;
      if (getNextPage) getNextPage();
      };
      // Perform a nearby search.
      service.nearbySearch(
      {location: pos, radius: 5000, type: ['doctor']},
      function(results, status, pagination) {
      if (status !== 'OK') return;
      createMarkers(results);
      moreButton.disabled = !pagination.hasNextPage;
      console.log("neraby serach");
      getNextPage = pagination.hasNextPage && function() {
      pagination.nextPage();
      };
      });
      }
      function createMarkers(places) {
      var bounds = new google.maps.LatLngBounds();
      var placesList = document.getElementById('places');
      console.log("enter Create Markets");
      for (var i = 0, place; place = places[i]; i++) {
      var image = {
      url: place.icon,
      size: new google.maps.Size(71, 71),
      origin: new google.maps.Point(0, 0),
      anchor: new google.maps.Point(17, 34),
      scaledSize: new google.maps.Size(25, 25)
      };
      var marker = new google.maps.Marker({
      map: map,
      icon: image,
      title: place.name,
      position: place.geometry.location
      });
      var li = document.createElement('li');
      li.textContent = place.name;
      placesList.appendChild(li);
      bounds.extend(place.geometry.location);
      }
      map.fitBounds(bounds);
      }
      function handleLocationError(browserHasGeolocation, infoWindow, pos) {
      infoWindow.setPosition(pos);
      infoWindow.setContent(browserHasGeolocation ?
      'Error: The Geolocation service failed.' :
      'Error: Your browser doesnt support geolocation.');
      infoWindow.open(map);
      }
    script(src='https://maps.googleapis.com/maps/api/js?key=AIzaSyAFrk57MQn5IqdNnK_DEfwHO-ksk-xGi4U&libraries=places&callback=initMap', async='', defer='')
